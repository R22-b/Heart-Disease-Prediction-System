services:
  - type: web
    name: Heart-Disease-Prediction-System
    runtime: docker
    branch: main
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "5000"
